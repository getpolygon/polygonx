version: "1"
packages:
  - name: "postgres_codegen"
    engine: "postgresql"
    emit_json_tags: true
    queries: "sql/queries/"
    schema: "sql/migrations/"
    path: "internal/gen/postgres_codegen/"
